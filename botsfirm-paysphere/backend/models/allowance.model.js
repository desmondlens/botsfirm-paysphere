// allowance.model.js
// Accessor for the `allowances` table.
// Fields: id, tenant_id, employee_id (nullable for tenant-wide defaults),
// name, type (fixed|percentage), amount, taxable (bool), recurring (bool), effective_from, effective_to.
// To be implemented.
