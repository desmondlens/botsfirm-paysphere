// user.model.js
// Accessor for the `users` table.
// Fields: id, tenant_id (nullable for super_admin), role, email (nullable for employee),
// username (used by employees), password_hash, last_login_at, failed_login_count, locked_until.
// To be implemented.
