// deduction.model.js
// Accessor for the `deductions` table.
// Fields: id, tenant_id, employee_id, name, type (fixed|percentage),
// amount, pre_tax (bool), recurring (bool), balance_remaining (for loans),
// effective_from, effective_to.
// To be implemented.
