// payslip.model.js
// Accessor for the `payslips` table.
// One row per employee per payroll run.
// Fields: id, tenant_id, payroll_run_id, employee_id, gross, allowances_total,
// deductions_total, taxable_income, paye, sdl, net, ytd_gross, ytd_paye, pdf_path.
// To be implemented.
